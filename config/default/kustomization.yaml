namespace: k8s-lb-controller-system
namePrefix: k8s-lb-controller-

resources:
- ../rbac
- ../manager
- metrics_service.yaml

# Optional: enable this only when the Prometheus Operator CRDs are installed.
#- ../prometheus
