namespace: k8s-lb-controller-system
namePrefix: k8s-lb-controller-

resources:
- ../rbac
- ../manager
- metrics_service.yaml

# Uncomment to install the optional ServiceMonitor when the Prometheus Operator CRDs are available.
#- ../prometheus
